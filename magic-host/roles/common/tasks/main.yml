---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install base packages
  ansible.builtin.apt:
    name:
      - curl
      - git
      - ca-certificates
      - htop
      - jq
      - unzip
      - nvme-cli
      - mc
      - sqlite3
      - qrencode
      - uuid-runtime
    state: present

- name: Set timezone to UTC
  community.general.timezone:
    name: UTC

- name: Install unattended-upgrades package
  ansible.builtin.apt:
    name: unattended-upgrades
    state: present

- name: Enable unattended-upgrades (security updates only, not enforced)
  ansible.builtin.copy:
    dest: /etc/apt/apt.conf.d/20auto-upgrades
    content: |
      APT::Periodic::Update-Package-Lists "1";
      APT::Periodic::Unattended-Upgrade "0";
    owner: root
    group: root
    mode: "0644"

- name: Persist host file watcher limits for Kubernetes workloads
  ansible.builtin.copy:
    dest: /etc/sysctl.d/90-ai-appliance-inotify.conf
    content: |
      fs.inotify.max_user_instances = 1024
      fs.inotify.max_user_watches = 1048576
      fs.inotify.max_queued_events = 32768
    owner: root
    group: root
    mode: "0644"

- name: Apply host file watcher limits for the current boot
  ansible.builtin.command:
    argv:
      - /sbin/sysctl
      - --load
      - /etc/sysctl.d/90-ai-appliance-inotify.conf
  changed_when: false

# NOPASSWD:ALL is intentionally broad for the MVP so that the converge runner
# can use 'become: true'
# without a password prompt.  This mirrors the sudo rule that cloud-init
# creates for the 'ai' user during first-boot (see magic-installer/user-data).
# Restrict to specific commands in a production hardening step.
- name: Grant ansible user passwordless sudo
  ansible.builtin.copy:
    dest: /etc/sudoers.d/ansible-nopasswd
    content: "{{ ansible_facts['user_id'] }} ALL=(ALL) NOPASSWD:ALL\n"
    owner: root
    group: root
    mode: "0440"
    validate: /usr/sbin/visudo -cf %s
