[defaults]
# ansible-pull checks out the repo to a working directory and runs
# ansible-playbook from there. This ensures the roles directory relative
# to the repo root is always found, regardless of which playbook is used.
roles_path = magic-host/roles
inventory = magic-host/inventory/localhost.yml
