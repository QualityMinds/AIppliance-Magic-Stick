apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: identity-pilot
  namespace: flux-system
spec:
  dependsOn:
    - name: envoy-gateway
  interval: 10m0s
  path: ./magic-cluster/platform/identity
  postBuild:
    substitute:
      var_substitution_enabled: "true"
    substituteFrom:
      - kind: ConfigMap
        name: ai-appliance-settings
        optional: true
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  timeout: 15m0s
