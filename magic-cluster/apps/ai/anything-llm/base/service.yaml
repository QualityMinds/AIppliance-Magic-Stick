apiVersion: v1
kind: Service
metadata:
  name: anything-llm
  namespace: ai
  labels:
    app: anything-llm
spec:
  type: ClusterIP
  selector:
    app: anything-llm
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
