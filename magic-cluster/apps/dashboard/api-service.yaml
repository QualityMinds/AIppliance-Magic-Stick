apiVersion: v1
kind: Service
metadata:
  name: ai-appliance-dashboard-api
  namespace: identity-system
  labels:
    app: ai-appliance-dashboard-api
    app.kubernetes.io/component: api
spec:
  selector:
    app: ai-appliance-dashboard-api
  ports:
    - name: api
      port: 8080
      targetPort: api
