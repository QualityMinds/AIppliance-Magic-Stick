apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: paperclip-operator
  namespace: flux-system
spec:
  interval: 10m0s
  targetNamespace: paperclip-operator-system
  chart:
    spec:
      chart: paperclip-operator
      version: "0.18.0"
      sourceRef:
        kind: HelmRepository
        name: paperclip-operator
        namespace: flux-system
  install:
    createNamespace: true
  upgrade:
    force: true
  values:
    crds:
      install: true
      keep: true
    image:
      repository: ghcr.io/qualityminds/magicstick-paperclip-operator
      tag: sha-1f9867b542b9b7b10fbc77f631a97a7cb25324be
