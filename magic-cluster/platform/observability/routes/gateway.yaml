apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-identity-observability
  namespace: observability
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: identity-system
  to:
    - group: ""
      kind: Service
      name: observability-kube-prometheus-stack-grafana
    - group: ""
      kind: Service
      name: observability-kube-prometheus-stack-prometheus
    - group: ""
      kind: Service
      name: observability-kube-prometheus-stack-alertmanager
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: static-grafana-local
  namespace: identity-system
  annotations:
    lab42.io/mdns.enabled: "true"
spec:
  parentRefs:
    - name: identity-pilot
      sectionName: https
  hostnames:
    - "grafana.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}"
  rules:
    - backendRefs:
        - name: observability-kube-prometheus-stack-grafana
          namespace: observability
          port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: static-prometheus-local
  namespace: identity-system
  annotations:
    lab42.io/mdns.enabled: "true"
spec:
  parentRefs:
    - name: identity-pilot
      sectionName: https
  hostnames:
    - "prometheus.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}"
  rules:
    - backendRefs:
        - name: observability-kube-prometheus-stack-prometheus
          namespace: observability
          port: 9090
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: static-alertmanager-local
  namespace: identity-system
  annotations:
    lab42.io/mdns.enabled: "true"
spec:
  parentRefs:
    - name: identity-pilot
      sectionName: https
  hostnames:
    - "alertmanager.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}"
  rules:
    - backendRefs:
        - name: observability-kube-prometheus-stack-alertmanager
          namespace: observability
          port: 9093
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: static-observability-local-callback
  namespace: identity-system
spec:
  parentRefs:
    - name: identity-pilot
      sectionName: https
  hostnames:
    - "${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}"
  rules:
    - matches:
        - path:
            type: Exact
            value: /oauth2/callback/static-observability-local
      backendRefs:
        - name: observability-kube-prometheus-stack-grafana
          namespace: observability
          port: 80
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: static-observability-local-sso
  namespace: identity-system
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: static-grafana-local
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: static-prometheus-local
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: static-alertmanager-local
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: static-observability-local-callback
  oidc:
    provider:
      issuer: "https://id.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}/realms/magicstick"
      authorizationEndpoint: "https://id.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}/realms/magicstick/protocol/openid-connect/auth"
      tokenEndpoint: http://keycloak.identity-system.svc.cluster.local:8080/realms/magicstick/protocol/openid-connect/token
      endSessionEndpoint: "https://id.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}/realms/magicstick/protocol/openid-connect/logout"
    clientID: magicstick-human-gateway-local
    clientSecret:
      name: magicstick-human-gateway-client
    redirectURL: "https://${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}/oauth2/callback/static-observability-local"
    logoutPath: /logout
    cookieDomain: "${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}"
    cookieNames:
      accessToken: MagicStickAccessToken
      idToken: MagicStickIDToken
    forwardAccessToken: true
  jwt:
    providers:
      - name: keycloak
        issuer: "https://id.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}/realms/magicstick"
        remoteJWKS:
          uri: http://keycloak.identity-system.svc.cluster.local:8080/realms/magicstick/protocol/openid-connect/certs
          backendRefs:
            - name: keycloak
              port: 8080
        extractFrom:
          cookies:
            - MagicStickAccessToken
  authorization:
    defaultAction: Deny
    rules:
      - name: required-magicstick-viewer
        action: Allow
        principal:
          jwt:
            provider: keycloak
            claims:
              - name: realm_access.roles
                valueType: StringArray
                values:
                  - magicstick-viewer
                  - magicstick-operator
                  - magicstick-admin
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: static-grafana-public
  namespace: identity-system
spec:
  parentRefs:
    - name: identity-pilot
      sectionName: https
  hostnames:
    - "grafana.${AI_APPLIANCE_DOMAIN:=magicstick.example.com}"
  rules:
    - backendRefs:
        - name: observability-kube-prometheus-stack-grafana
          namespace: observability
          port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: static-prometheus-public
  namespace: identity-system
spec:
  parentRefs:
    - name: identity-pilot
      sectionName: https
  hostnames:
    - "prometheus.${AI_APPLIANCE_DOMAIN:=magicstick.example.com}"
  rules:
    - backendRefs:
        - name: observability-kube-prometheus-stack-prometheus
          namespace: observability
          port: 9090
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: static-alertmanager-public
  namespace: identity-system
spec:
  parentRefs:
    - name: identity-pilot
      sectionName: https
  hostnames:
    - "alertmanager.${AI_APPLIANCE_DOMAIN:=magicstick.example.com}"
  rules:
    - backendRefs:
        - name: observability-kube-prometheus-stack-alertmanager
          namespace: observability
          port: 9093
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: static-observability-public-callback
  namespace: identity-system
spec:
  parentRefs:
    - name: identity-pilot
      sectionName: https
  hostnames:
    - "${AI_APPLIANCE_DASHBOARD_HOST:=magicstick.example.com}"
  rules:
    - matches:
        - path:
            type: Exact
            value: /oauth2/callback/static-observability-public
      backendRefs:
        - name: observability-kube-prometheus-stack-grafana
          namespace: observability
          port: 80
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: static-observability-public-sso
  namespace: identity-system
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: static-grafana-public
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: static-prometheus-public
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: static-alertmanager-public
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: static-observability-public-callback
  oidc:
    provider:
      issuer: "https://id.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}/realms/magicstick"
      authorizationEndpoint: "https://id.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}/realms/magicstick/protocol/openid-connect/auth"
      tokenEndpoint: http://keycloak.identity-system.svc.cluster.local:8080/realms/magicstick/protocol/openid-connect/token
      endSessionEndpoint: "https://id.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}/realms/magicstick/protocol/openid-connect/logout"
    clientID: magicstick-human-gateway-local
    clientSecret:
      name: magicstick-human-gateway-client
    redirectURL: "https://${AI_APPLIANCE_DASHBOARD_HOST:=magicstick.example.com}/oauth2/callback/static-observability-public"
    logoutPath: /logout
    cookieDomain: "${AI_APPLIANCE_DOMAIN:=magicstick.example.com}"
    cookieNames:
      accessToken: MagicStickAccessToken
      idToken: MagicStickIDToken
    forwardAccessToken: true
  jwt:
    providers:
      - name: keycloak
        issuer: "https://id.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}/realms/magicstick"
        remoteJWKS:
          uri: http://keycloak.identity-system.svc.cluster.local:8080/realms/magicstick/protocol/openid-connect/certs
          backendRefs:
            - name: keycloak
              port: 8080
        extractFrom:
          cookies:
            - MagicStickAccessToken
  authorization:
    defaultAction: Deny
    rules:
      - name: required-magicstick-viewer
        action: Allow
        principal:
          jwt:
            provider: keycloak
            claims:
              - name: realm_access.roles
                valueType: StringArray
                values:
                  - magicstick-viewer
                  - magicstick-operator
                  - magicstick-admin
