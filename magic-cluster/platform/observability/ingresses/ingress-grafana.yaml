apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: observability
  labels:
    app: grafana
  annotations:
    lab42.io/mdns.enabled: "true"
    lab42.io/mdns.name: "grafana.${AI_APPLIANCE_MDNS_NAME:=magicstick}"
    lab42.io/mdns.host: "grafana.${AI_APPLIANCE_MDNS_NAME:=magicstick}"
spec:
  ingressClassName: nginx
  rules:
    - host: "grafana.${AI_APPLIANCE_DOMAIN:=magicstick.example.com}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: observability-kube-prometheus-stack-grafana
                port:
                  number: 80
    - host: "grafana.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: observability-kube-prometheus-stack-grafana
                port:
                  number: 80
