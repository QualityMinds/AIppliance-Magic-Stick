apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: alertmanager
  namespace: observability
  labels:
    app: alertmanager
  annotations:
    lab42.io/mdns.enabled: "true"
    lab42.io/mdns.name: "alertmanager.${AI_APPLIANCE_MDNS_NAME:=magicstick}"
    lab42.io/mdns.host: "alertmanager.${AI_APPLIANCE_MDNS_NAME:=magicstick}"
spec:
  ingressClassName: nginx
  rules:
    - host: "alertmanager.${AI_APPLIANCE_DOMAIN:=magicstick.example.com}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: observability-kube-prometheus-stack-alertmanager
                port:
                  number: 9093
    - host: "alertmanager.${AI_APPLIANCE_MDNS_DOMAIN:=magicstick.local}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: observability-kube-prometheus-stack-alertmanager
                port:
                  number: 9093
