apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prometheus
  namespace: observability
  labels:
    app: prometheus
  annotations:
    lab42.io/mdns.enabled: "true"
    lab42.io/mdns.name: "prometheus"
spec:
  ingressClassName: nginx
  rules:
    - host: prometheus.example.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: observability-kube-prometheus-stack-prometheus
                port:
                  number: 9090
