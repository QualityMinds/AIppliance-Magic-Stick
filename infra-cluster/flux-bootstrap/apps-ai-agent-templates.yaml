apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-ai-agent-templates
  namespace: flux-system
spec:
  dependsOn:
    - name: apps-ai-kubeopencode
  interval: 10m0s
  path: ./infra-cluster/apps-ai-agent-templates
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
