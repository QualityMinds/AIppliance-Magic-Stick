apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-ai
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure-ai
  interval: 10m0s
  path: ./infra-cluster/apps-ai
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
