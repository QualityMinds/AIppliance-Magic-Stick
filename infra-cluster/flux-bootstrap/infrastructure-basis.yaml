apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-basis
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./infra-cluster/infrastructure-basis
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
