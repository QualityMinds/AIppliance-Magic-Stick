apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anything-llm-storage
  namespace: ai
  labels:
    app: anything-llm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
