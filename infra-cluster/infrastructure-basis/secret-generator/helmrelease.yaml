apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubernetes-secret-generator
  namespace: flux-system
spec:
  interval: 10m0s
  targetNamespace: flux-system
  chart:
    spec:
      chart: kubernetes-secret-generator
      version: "3.4.1"
      sourceRef:
        kind: HelmRepository
        name: mittwald
        namespace: flux-system
  values:
    watchNamespace: ""
    image:
      registry: ghcr.io
      repository: tignis/kubernetes-secret-generator
      tag: "3.4.1"
