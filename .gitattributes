magic-cluster/platform/basis/kdns/patches/*.patch -whitespace
magic-cluster/platform/gpu/amd-gpu-operator/patches/*.patch -whitespace
*.pdf binary
*.pptx binary
# Preserve upstream license text verbatim, including whitespace and RST headings.
# Real merge conflicts in these files use longer markers instead of Authors' =======.
licenses/third-party/*.txt -whitespace conflict-marker-size=32
